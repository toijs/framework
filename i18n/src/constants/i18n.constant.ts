/** Metadata key used to expose the i18n singleton on the launcher. */
export const METADATA_I18N = "metadata.i18n";

/** Locale used when a key is missing from the requested locale. */
export const DEFAULT_FALLBACK_LOCALE = "en";
